from FireApi import Client, Request


fn main() raises -> None:
    let client = Client(
        host_name="127.0.0.1",
        port=8080,
    )

    let request = Request(
        body="hello, world",
    )
    
    let response = client.send_request(request)

